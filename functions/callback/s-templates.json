{
    "requestCallbackTemplate" : {
         "application/json": {
            "code": "$input.params('code')",
            "secret": "${fb_secret}",
            "app_id": "${app_id}"
         }
    }
}